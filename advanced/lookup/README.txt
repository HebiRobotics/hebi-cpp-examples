Provides more examples of usage of the lookup class.

Also includes a simple command-line argument parser for lookup.  This allows one to write code that can run on HEBI modules with different names, without requiring recompilation.
